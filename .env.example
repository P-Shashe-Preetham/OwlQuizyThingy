# Firebase Service Account (base64-encoded JSON)
FIREBASE_SERVICE_ACCOUNT=

# CORS origin for WebSocket server (optional, defaults to "*")
# CORS_ORIGIN=https://yourdomain.com

# Host / Manager Password (optional override; default is read from config/game.json)
# MANAGER_PASSWORD=admin123

